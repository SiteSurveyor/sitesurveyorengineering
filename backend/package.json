{
  "name": "sitesurveyor-backend-runner",
  "private": true,
  "scripts": {
    "build": "npm --prefix ../frontend run build",
    "dev": "npm --prefix ../frontend run dev",
    "tauri": "npx --prefix ../frontend tauri"
  }
}
