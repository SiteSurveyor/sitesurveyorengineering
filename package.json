{
  "name": "sitesurveyorengineering",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend run build",
    "dev": "npm --prefix frontend run dev"
  },
  "devDependencies": {
    "supabase": "^2.98.0"
  }
}
